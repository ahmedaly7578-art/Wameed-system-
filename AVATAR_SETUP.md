
# صور البروفايل 📷 (Avatars)

## اللي اتضاف
- كل مستخدم يقدر يدوس على صورته/حروفه في أسفل السايدبار ⇦ تفتح نافذة "صورتي
  الشخصية" يختار منها صورة، تترفع، وتتحفظ **لكل المستخدمين** (مش بس عنده).
- الصورة بتحل محل الحروف (Initials) في كل مكان في النظام: السايدبار، جدول
  الفريق، بطاقات المهام، التاسكات، الفولو أب... إلخ.

## المطلوب منك في Supabase (خطوتين بس)
```bash
supabase db push               # أو نفّذ محتوى supabase/migrations/003_avatars.sql
                                # يدوي من SQL Editor لو مش شغال CLI عندك — ده بيضيف
                                # عمود avatar_url وبيعمل Storage bucket اسمه "avatars"

supabase functions deploy avatar-upload
supabase secrets set AVATAR_UPLOAD_SECRET='wameed-avatar-4d9f2c7b1e5a8036'
```
لاحظ: الـ secret ده **لازم يطابق** القيمة المكتوبة في `App.jsx` (المتغير
`AVATAR_API_SECRET`) عشان الرفع يشتغل من غير ما تلمس كود. مفيش داعي تحط
`SUPABASE_SERVICE_ROLE_KEY` يدوي — هو موجود تلقائي لأي Edge Function.

## المطلوب منك في الشيت
**مفيش أي حاجة** — الصور دي جوه Supabase بس، مالهاش علاقة بالشيت خالص.

## ملاحظات
- أقصى حجم للصورة 5 ميجا (اتضبطت من داخل الـ Edge Function نفسها).
- الصورة بتتخزن باسم `<user-id>.<الامتداد>` في bucket اسمه `avatars`، ولو
  المستخدم غيّرها تاني بتستبدل القديمة بنفس المكان.
- الـ bucket شغال **public read** (أي حد معاه اللينك يقدر يشوف الصورة) —
  ده طبيعي وضروري عشان الصور تظهر في المتصفح، لكن معناه إن الصور مش خاصة/سرية.
